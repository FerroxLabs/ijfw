# Gitleaks ignore -- relative paths match the preflight gate's `--no-git` mode.
#
# All entries are synthetic / redacted / archived test fixtures, not real
# secrets. The preflight gate (installer/src/preflight/gates/gitleaks.js)
# scans the working tree with `--no-git`, so fingerprints are
# `<path>:<rule-id>:<line>`.

# Cross-audit stderr captures contain redacted webhook URLs from the
# auditor pipeline. Already redacted at capture time; surfaced here as
# defense-in-depth on the ignore list.
.ijfw/cross-audit/codex.err:slack-webhook-url:864
.ijfw/cross-audit/archive/2026-04-14T2030-r2-codex-gemini/codex.stderr:slack-webhook-url:1000
.ijfw/cross-audit/archive/2026-04-14T2030-r2-codex-gemini/codex.stderr:slack-webhook-url:2503

# Audit briefs occasionally quote a redactor input verbatim to demonstrate
# scrub behavior. Synthetic, not a real key.
.planning/audit-briefs/GA-CA4-claude-swarm-fresh.md:stripe-access-token:76

# Synthetic test fixtures in the ingest-time redaction test
# (mcp-server/test-ingest-redaction.js). The strings match Stripe/OpenAI key
# shapes because the test verifies the redactor scrubs them before INSERT;
# the bodies are obviously fake (TESTABC123... padding).
mcp-server/test-ingest-redaction.js:stripe-access-token:52

# LongMemEval-S benchmark dataset (ICLR 2025) ships with synthetic
# generic-API-key shapes inside its question corpus. Treated as upstream
# test data, not a project secret.
mcp-server/test/fixtures/longmemeval-s/longmemeval_s_cleaned.json:generic-api-key:101515

# Historic gitleaks finding from a prior commit, kept for back-compat.
3ef884ee206b1fd92651b6fdfae2fd2206778448:mcp-server/test-redactor.js:slack-webhook-url:145

# v1.4.3 handoff documents the Ed25519 registry meta-key FINGERPRINT
# (sha256 of the public key). The public key is compiled into
# mcp-server/src/extension-registry.js as IJFW_REGISTRY_META_KEY_PEM and
# is also published openly at https://therealseandonahoe.gitlab.io/ijfw/
# registry/publishers/v1.json. By definition not a secret — high entropy
# triggers generic-api-key. Same string appears in v1.4.1 ship memory
# (~/.claude/projects/-Users-seandonahoe-dev-ijfw/memory/v141-shipped.md)
# but that's outside the scan root.
.planning/1.4.3/HANDOFF-1.4.3.md:generic-api-key:711
/Users/seandonahoe/dev/ijfw/.planning/1.4.3/HANDOFF-1.4.3.md:generic-api-key:711

# Deliberately fake token on line 110 of the redaction unit test (a synthetic
# placeholder assigned to a `token` const in the test fixture). Not a real secret.
# Both relative and absolute fingerprints listed: the preflight gate scans with an
# absolute --source, so gitleaks emits the absolute-path fingerprint form.
mcp-server/test/profile-edit-delta.test.mjs:generic-api-key:110
/Users/seandonahoe/dev/ijfw/mcp-server/test/profile-edit-delta.test.mjs:generic-api-key:110
